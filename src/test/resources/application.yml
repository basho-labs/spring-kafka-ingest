hachiman:
  ingest:
    group: ${INGEST_GROUP:default}
    default:
      config: '{"name": "hachiman-test",
      "kafka": {"topic": "hachiman-test", "zookeepers": ["127.0.0.1:2181"]},
      "riak": {"bucket": "test-bucket", "hosts": ["192.168.161.16:8087"],
      "schema":"timestamp,varchar,double,double,varchar,double"}}'
      kafka:
        brokers: 'localhost:9092'
